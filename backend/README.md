# Exercice 1 – Spring Boot "Hello World"

## Objectif

Créer une application Java Spring Boot simple qui affiche "Hello world" sur l’URL racine (`/`).

## Lancer l'application

Depuis le dossier `backend`, exécuter :

```bash
mvn spring-boot:run